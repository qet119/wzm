export interface Wzm {
    sfzhm?: string; // 身份证号码
    sqrxm?: string; // 申请人姓名
    zmmc?: string; // 证明名称
    lxfs?: string; // 联系方式
    xb?: string; // 性别
    jg?: string; // 籍贯
    attachs?: string[]; // 上传材料 (假设为字符串数组，根据实际情况调整)
  }